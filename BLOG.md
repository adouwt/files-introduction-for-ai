# 我把“给 AI 用的代码索引器”做成了一个可复用的 npm CLI

在 AI 辅助开发越来越普及的今天，一个高频痛点是：**AI 不理解你的项目上下文**。
于是我做了一个轻量工具：`files-introduction-for-ai`，并把它整理成了可发布、可复用的 npm CLI —— `ai-file-indexer`。

它解决的核心问题很直接：把代码仓库自动整理成适合 AI 消化的结构化索引。

---

## 这个工具解决了什么问题？

很多团队都遇到过这些情况：

- 代码库很大，AI 只能“盲猜”业务结构
- 每次让 AI 介入都要重复解释模块关系
- 变更后没有增量更新，索引很快过期
- 提交代码时缺少“上下文产物”的自动更新机制

这个工具的目标就是把这些动作标准化：

1. 初始化项目配置
2. 自动生成文件级 + 模块级索引
3. 支持全量与增量模式
4. 可挂接到 Git pre-commit，随提交自动更新

---

## 项目能力概览

这个 CLI 主要有 3 个命令入口：

- `ai-file-indexer init`  
  初始化配置与 hooks 模板，并补齐 `package.json` 的常用脚本
- `ai-file-indexer index --full|--incremental [--stage-output]`  
  执行索引（全量或基于 staged 变更的增量）
- `ai-file-indexer hooks setup`  
  自动设置 Git 使用 `.githooks` 目录作为 hooks 路径

---

## 它是怎么工作的？

### 1) 初始化阶段（`init`）

执行初始化后，目标项目会得到：

- `.ai-indexer.config.json`（索引配置模板）
- `.githooks/pre-commit`（提交前触发索引的脚本）
- `package.json` 中补齐索引相关 scripts

这一步的价值是：**把接入成本降到最低**，开箱即可用。

### 2) 索引阶段（`index`）

索引流程会做这些事：

- 读取配置（扩展名白名单、排除目录、输出目录、LLM 配置等）
- 根据模式选择目标文件  
  - `--full`：扫描 git 跟踪文件  
  - `--incremental`：只处理当前 staged 的变更文件
- 提取文件基础结构信息  
  - 语言类型  
  - 函数名（正则抽取）  
  - 依赖信息（import/require 等）
- 调用大模型生成摘要（支持 Qwen；无 API Key 时走兜底摘要）
- 产出多份索引文件（JSON + Markdown，文件级 + 模块级）

最终产物默认输出到 `.ai/` 目录，适合被 AI 或人类直接消费。

### 3) 提交集成（`hooks setup`）

通过 hooks 设置后，Git 提交可以和索引更新形成固定流程。
如果配合 `--stage-output`，索引产物还能自动加入暂存区，避免“代码改了，文档没跟上”。

---

## 产物设计：为什么同时做文件级和模块级？

我在设计时刻意拆成两层：

- **文件级索引**：细粒度，适合定位具体实现
- **模块级索引**：聚合视角，适合理解业务边界和目录职责

这样 AI 处理项目时，不会只盯单文件，也能先看模块再下钻细节。

---

## 配置思路（`.ai-indexer.config.json`）

配置项重点包括：

- `includeExtensions`：纳入索引的文件类型
- `excludeGlobs`：忽略目录（如 `node_modules`、`dist`）
- `outputDir` 与各类输出文件名
- `llm.provider/baseUrl/model`
- `maxFileCharsForLlm`：控制输入 token 成本

这让工具在不同仓库里都能“按需裁剪”。

---

## 为什么做成 npm CLI？

从“脚本”升级为“CLI”的核心收益是工程化：

- 统一入口，降低团队认知成本
- 可发布、可复用，不依赖单仓库私有脚本
- 便于接入 CI/CD 和多项目复制
- 对外扩展更自然（未来可加 provider、解析器、输出格式）

---

## 适用场景

我认为这个工具尤其适合：

- 多人协作、业务模块较多的中大型仓库
- 希望把 AI 接入流程标准化的团队
- 需要“提交即更新上下文索引”的研发流程
- 想降低新人理解项目成本的项目组

---

## 后续可迭代方向

下一步我会考虑这几件事：

- 更精细的语言解析（减少纯正则抽取误差）
- 支持更多模型提供商与本地模型
- 增强模块关系图（调用链/依赖拓扑）
- 增加 CI 模式（PR 自动更新并校验索引）

---

## 一句话总结

`ai-file-indexer` 本质上是在做一件事：
**把“给 AI 解释项目”从一次性对话，变成可持续、可自动化的工程资产。**

---

## 相关链接

- GitHub 仓库：https://github.com/adouwt/files-introduction-for-ai
